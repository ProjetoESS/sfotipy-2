import { MusicsFilterPipe } from './musics-filter.pipe';

describe('MusicsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MusicsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
