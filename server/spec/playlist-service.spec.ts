import 'jasmine'
import { PlaylistService } from '../src/playlist-service'
import { Playlist } from '../../common/playlist'

describe('PlaylistService', () => {
    var playlistService : PlaylistService;

    beforeEach(() => playlistService = new PlaylistService());

    
});